lib.path = "main.rs"

[package]
name = "stepwise"
description = """
stepwise
"""
version = "0.0.1"

license = "Apache-2.0"
edition = "2021"

[dev-dependencies]
tokio = { version = "1", features = ["full"] }

[dependencies]
async-trait = "0.1"
